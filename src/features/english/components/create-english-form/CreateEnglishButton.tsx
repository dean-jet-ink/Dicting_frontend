import { Plus } from "lucide-react";

import Button from "@/components/button/Button";

const CreateEnglishButton = () => {
  return <Button icon={<Plus />}>Create</Button>;
};

export default CreateEnglishButton;
